# Strata — Roadmap thực thi

> **Repo:** `LampNetCloud/Strata` (Rust) · **Cập nhật:** 2026-07-30
> **Strata** = tầng lưu trữ tiến hóa của MagicLamp: chuỗi version hash-link + MMR + `state_root` field-Merkle + anchor on-chain + audit-log.
> **Spec nguồn:** `spec/_CONTRACT.md` (khế ước giao diện) + `spec/Strata-Feat/Math/Tech/API.md`.
> **Theo dõi công việc:** issue `#1` (S1) · `#2` (S2) · `#3` (S3) · `#24` (S7: CI + fmt drift).

---

## 0. Bối cảnh

- Module hạ tầng dùng chung xuyên nền tảng (OriLife, ProofChat, AladinWork, VeData…) qua **Strata API**.
- Nối VeData: `Strata.anchor → Mosaic` (CIP-68) — Strata neo on-chain qua Mosaic của VeData (issue S1).
- Bất biến: INV-E1..E9 (append-only; INV-E5 CID không lộ loại nội dung; INV-E6 field-proof ghép ZK; INV-E7 anchor chống rollback).

---

## 1. Baseline / dependency

- **PR #4** (`chore(deps): repoint lampnet-merkle-anchor → repo tách Anchor + pin rev`) — ✅ **MERGED 2026-07-04.**
- **PR #8** (`docs(spec): addendum §5.3/§8.3 + chốt Settlement + nghiệm thu T1–T6 Preview`) — ✅ **MERGED 2026-07-09** (nền spec; Addendum S3.1 tại `cfa4970`).
- **PR #5** (S2 DerivedIndex) — ✅ **MERGED 2026-07-09** (vá review vòng 2: comment test build_log).
- **PR #7** (S3 BatchCheckpoint) — ✅ **MERGED 2026-07-09** (vá vòng 2 + Addendum S3.1: DoS parse_batch, seq liên tục, van b′).
- **PR #6** (S1 AnchorSink) — ✅ **MERGED 2026-07-09** (vòng 2: `new_unverified_for_tests` + WARN anchor-thật-hỏng + test biên `prove_version_at`).
- **Thứ tự merge anh Đức chốt:** #8 → #5 → #7 → #6 — đã MERGE HẾT đúng thứ tự (07-09).
- **PR #10** (S4 Composite + Tabular Merkle Sum Tree) — ✅ **MERGED 2026-07-16** (squash `d899ae2`). Cứu từ nhánh agent cũ; review đạt (build 0 warning, 121 xanh, domain-tag `LN/STRATA/sum/*` tách miền, red-team giả child_ref/ref_id). Nit `verify_row_sum` để lại.
- **Issue #9 / PR #11** (refactor `derived_index`) — ✅ **MERGED 2026-07-16** (squash `ece19f7`). Điểm (1): gỡ field `seq` thừa trong `CompositeFieldProof`, `verify_composite` dùng thẳng `version.seq`. Điểm (2) `prove_field_at_version` để **backlog** (chưa caller thật cần). Issue đã đóng.
- **S1 follow-up — Codec Settlement + hợp nhất `AnchoredTable`** — ✅ **CODE-COMPLETE 2026-07-20** (branch `thinh/strata-settlement-codec`, `STRATA-S1-REPORT.md §9`). Graft từ rev `72f7135`: (1) codec label 1234 CBOR-raw `[{t,a}]` chunk-64B-bijective + decode strict/lenient vào **crate lõi** `src/settlement.rs` (enum `SettlementRecord`); (2) `SettlementSink` thuần generic dùng CHUNG `AnchoredTable`/`verify_resolved` (KHÔNG `AnchoredLog` song song); (3) I/O Blockfrost + `TsSubmitter` tách **crate riêng** `lampnet-anchor-io` (repo → workspace). `cargo test --workspace` **146 pass**, clippy 0 warning. Lấp nốt "việc chính còn lại của S1".
- **S5 — Daemon + HTTP §3 (`lampnet-strata-node`)** — ✅ **MERGED 2026-07-25** (PR #13, `STRATA-S5-REPORT.md`). Workspace member thứ ba `node/`: 8 route §3 + bảng lỗi §3.1 đủ 11 biến thể, `KeyRegistry` (CHỐT-5), `ChainStore` khoá-theo-ref, cắm `AnchorSink` §4.1. Rebase lên main mới + dọn 3 nit trước khi land (report → `docs/`, gỡ dep `log` thừa, `?seq=` hỏng phải ra format lỗi của ta): `cargo test --workspace` **164 pass**, clippy 0 warning. **Đã chạy binary thật + smoke `curl` 8/8**. Lấp mảnh "ráp daemon thật + HTTP §3" treo từ S2. Treo: **issue #23** chốt thông điệp ký của `event kind=audit` (chữ ký không vào leaf ⇒ không root nào cam kết byte-layout).
- **Test-vector CHUNG Rust↔TS cho metadatum label 1234** — ✅ **PR #33 MERGED 2026-07-30** (`0fefc0b`). `submit.ts` dựng metadatum **độc lập** với `settlement.rs` (cùng layout `[{t,a}]` + luật chunk 64B, viết hai lần hai ngôn ngữ) ⇒ trước đó không gì bắt được lúc hai bên trôi khỏi nhau. Nay `apis/settlement-metadata.json` là đề chung (Rust sinh vì Rust giữ decoder), `tests/settlement_fixture.rs` khoá phía Rust, `test/fixture.test.ts` + `npm run test:fixture` khoá phía TS. 8 ca gồm biên **64B (cấm chunk)** và **65B (64+1)**. *Bài học ghi lại:* bản test đầu gộp "bytestring trần" với "mảng 1 chunk" nên **negative control vẫn xanh** — đúng malleability decoder từ chối; đã vá bằng cách giữ phân biệt ≤64B=chuỗi / >64B=mảng, rồi xác nhận 3 negative control đều đỏ. Cùng lớp gap `VeDataIO/Core#64`.
- **Issue #18 / PR #21 — trần cứng `<2³²` fail-loud canonical §1.7** (đồng bộ Spectra §1.9, PR `LampNetCloud/Spectra#65`) — ✅ **code MERGED 2026-07-30** (`4e9b200`): `assert!` trong `u32_be` chạy **cả release** + route count-prefix của `anchor_sink::AnchoredTable::to_bytes` qua `u32_be` (trước đó cast `as u32` trực tiếp, làm câu "van chung phủ hết" chưa đúng). Câu spec §1.7 quy tắc 3 **tách sang PR #27** (miền anh Đức) — PR #21 từng treo 6 ngày vì bọc code chung với 1 dòng spec. Report: `STRATA-CANONICAL-CAP-REPORT.md`.
- `lampnet-merkle-anchor` tách khỏi repo private `lampnet-hivemind` sang repo riêng **`LampNetCloud/Anchor`**; `Cargo.toml` repoint sang Anchor + pin git-rev. Không còn phụ thuộc quyền đọc `lampnet-hivemind`.
- `cargo build` / `cargo test` chạy được với Anchor git+rev — **65 test pass**, không đổi code Strata.

---

## 2. Milestone

| ID | Tên | Phần | Mức độ | Trạng thái |
|---|---|---|---|---|
| **S1** | Anchor adapter `Strata.anchor → Mosaic` (CIP-68) | Rust + on-chain | P0 | ✅ **CODE-COMPLETE + NEO ON-CHAIN THẬT (L1 Preview) + VÁ 2 VÒNG REVIEW** (`STRATA-S1-REPORT.md`) — `src/anchor_sink.rs`: `MosaicAnchorSink`/`AnchorSink` + datum CIP-68 §8.1 + **thread-token NFT one-shot** chống đầu độc + `publish_with_retry` + `AnchoredTable(ref_id,seq)` + `chain::prove_version_at`/`verify_resolved`. Phase 2: neo datum CIP-68 THẬT + validator Plutus V3 spend-recreate THẬT (INV-E7 on-chain) Preview. **PR #6** (base main, rebase sau #8/#5/#7). Vòng 2 (07-09): `new_unverified_for_tests()` + WARN anchor-thật-hỏng + test biên `prove_version_at` + gộp check version_hash. **111 pass / clippy sạch / fmt clean.** ✅ **MERGED 2026-07-09.** **Follow-up codec Settlement + hợp nhất `AnchoredTable`** (backend mặc định = Settlement theo PR #8) → ✅ **CODE-COMPLETE 2026-07-20** (xem §1 + `STRATA-S1-REPORT.md §9`). **Audit hội đồng #14/#15/#16 + PR #17 (regression test) MERGED 2026-07-24.** **#14 (resolve flood-eviction, CAO):** hướng **A-opt `beacon_mode`** (con-trỏ-latest theo asset, native policy `sig(publisher)`) — **read-side + write-side CODE-COMPLETE, đã CHẠY LIVE Preview đầu-cuối** (mint→walk→resolve seq0→seq1, tx `41cffc9f`/`95a6ff00`; nhánh `thinh/strata-14-beacon-resolve` PR #19). `submit.ts` beacon-walk + `tsc` typecheck; `examples/resolve_beacon.rs`. **#14 XONG — PR #19 MERGED 2026-07-30** (`c141ba7`; 149 pass/clippy sạch/tsc 0). **Anh Đức chốt hướng (PR #19)**: (a) tách bạch 2 mức bảo đảm — beacon-native = miễn-nhiễm-flood nhưng đơn điệu VẪN tin khoá publisher (native policy chặn mint-lại, KHÔNG chặn di-chuyển ⇒ publisher/khoá-bị-chiếm vẫn đẩy beacon về seq thấp = rollback đã-xác-thực), còn **Mosaic A** (validator ép `seq_out>seq_in`) mới là INV-E7 độc-lập-khoá; (b) trần quy mô beacon per-ref_id ~10⁷ ref_id/publisher, lớn hơn cần **aggregated-root** (cây commit nhiều ref_id, proof O(log N)) — roadmap. §4 report đã áp; text canonical vào spec do anh Đức ghi. #16 (code-only) đã code trong PR #19 (`4a6f8b9`); #15 (spec payload) anh Đức xử cùng đợt spec — `STRATA-S1-BEACON-REPORT.md`. **Mới: issue #24** — repo chưa có CI nào + `fmt --check` đỏ 8 hunk trên main (rustfmt 1.9.0 drift, không pin toolchain); thứ tự pin → workflow → fmt-sweep → bật gate. |
| **S2** | DerivedIndex / columnar query engine | Rust thuần | P1 | ✅ **CODE-COMPLETE 2026-07-04** (`STRATA-S2-REPORT.md`) — module `src/derived_index.rs`: `VersionLog`/`Query`/`DerivedIndex`+`ColumnarIndex`/`brute_force` + **proof 2 tầng** `CompositeFieldProof`+`verify_composite` (ghép `state::prove_field` × `chain::prove_version`, bind qua `version.state_root`) + `InMemoryVersionLog` (state_fields off-chain, ràng buộc `build_state_root==version.state_root`). **7 test** (5 tiêu chí §8.2 + 2 negative), benchmark n∈{1e2..1e5} (build O(n), lookup_latest O(1)). Repo **71 pass / 0 warning / clippy sạch**. Không primitive mới, không write-back (§7.5). Còn: ráp daemon thật + HTTP §3 + Backend sign-off. |
| **S3** | BatchPolicy / checkpoint sub-MMR (4 tầng lưu) | Rust | P1 | ✅ **CODE-COMPLETE + VÁ 2 VÒNG REVIEW** (`STRATA-S3-REPORT.md`) — `src/batch.rs`: `Checkpoint`(sub-MMR)+`BatchPolicy`/`should_close`+`EpochAccumulator`+blob canonical(`serialize/parse_batch`)+`TwoTierProof`/`verify_two_tier(&StrataVersion)`. **PR #7** (base main, rebase sau #8). Vòng 1 (07-08): 8 mục `flush_max_age`+watermark+`max_entries`@push+payload-guard. Vòng 2 + **Addendum S3.1** (07-09): DoS `parse_batch` cap + `serialize_batch`→Result + header version byte + `entry_seq` LIÊN TỤC(gap) + arrival-ts + `max_epoch_bytes` van b′. **98 pass / clippy sạch / fmt clean.** ✅ **MERGED 2026-07-09.** |
| **S4** | Composite Strata (rừng cha–con) + Tabular Merkle Sum Tree | Rust | P1 | ✅ **CODE-COMPLETE + MERGED 2026-07-16** (`STRATA-S4-REPORT.md`) — `src/composite.rs`: `ChildRef`/`ParentChildProof`+`prove_child`/`link_two_tier`/`composite_state_root` (con = field `role→child_ref_id` 32B thuần trong `state_root` cha, **không primitive mới**, tái dùng field-proof; `link_two_tier` chỉ kiểm bước NỐI, bind gốc-con qua anchor con riêng). `src/tabular.rs`: `ColumnSumTree`/`RowSumProof`+`verify_row_sum`/`verify_row_sum_range` (bọc `sumtree` Anchor, tag `LN/STRATA/sum/{leaf,node}/v1` tách miền khỏi mmr/state/ver; `u128` tránh float). **10 test mới** (composite 4 + tabular 6, gồm red-team giả child_ref/ref_id + tamper value/range). **PR #10** review đạt → merge. Nit `verify_row_sum` (đưa proof `prove_range` b>a+1 vào → false, an toàn) để lại. |

| **S5** | Daemon + HTTP API §3 (`lampnet-strata-node`) | Rust (axum) | P1 | ✅ **MERGED 2026-07-25 (PR #13)** (`STRATA-S5-REPORT.md`) — crate `node/`: **8 route §3** (`create`/`version`/`event`/`head`/`version?at`/`proof/version`/`proof/field`/`anchor`) + **bảng lỗi §3.1 đủ 11 biến thể** `StrataError` + 6 lỗi mức-cửa; `trait KeyRegistry` `Did→pk` (CHỐT-5, 424 fail-closed); `ChainStore` khoá **theo từng ref** + giữ `state_fields` theo seq để dựng `prove_field` lịch sử (`?seq=`); cắm `AnchorSink` §4.1 (`DisabledSink`/`MemorySink`/`FailingSink`), **KHÔNG viết backend mới**. Thứ tự neo **kiểm → đẩy → chốt** (gọi `publish_anchor()` trước rồi backend hỏng sẽ làm ref chết vĩnh viễn — có test riêng). Ranh giới §0/§8.4 giữ nguyên: daemon không băm, không ký, không dựng tx. **164 pass workspace / clippy 0 warning / fmt clean** (sau rebase lên main mới), + smoke `curl` 8/8 trên binary thật. Treo **issue #23** (thông điệp ký `event kind=audit`). Còn: bền vững (in-memory), cắm backend neo thật, ghép S3/S4 + `DerivedIndex` vào đường đọc. |
| **S6** | Bền vững (persistence) + đường Mirage | Rust + cross-repo | **P0** | ⬜ **CHƯA BẮT ĐẦU.** `node/src/store.rs` hiện `HashMap` + `RwLock` **in-memory** — chính comment `store.rs:19` ghi "bền vững (đĩa/Mirage) là milestone sau" nhưng chưa có milestone nào; restart daemon = mất head/log/state_fields/anchor state. **Hai phần rời nhau:** (a) **Strata-side** — log + `state_fields` theo seq + `AnchorState` xuống đĩa (WAL/snapshot), `ChainStore` thành trait để cắm backend; (b) **cross-repo LampNet** — Tech §4.3 đòi Mirage có **mode thứ ba `EncryptedDistributed`**: hiện Vault = mã-hoá-KHÔNG-repair, Hybrid (mặc định) = KHÔNG-repair + plaintext, Symmetric = repair + plaintext ⇒ **không giao điểm "mã hoá ∧ repair"**. **Hệ quả phải ghi rõ, không hứa quá: tới khi Mirage có mode đó, content nhạy cảm KHÔNG đạt INV-E9.** *Xong khi:* kill + restart daemon giữ nguyên `head`/`proof/version`/`proof/field` (có test), blob đi Mirage qua trait, đường nhạy cảm phát cảnh báo `no-repair`. |
| **S7** | CI + property test (issue #24) | Hạ tầng | **P0** | 🟡 **ĐANG LÀM — 2/3 bước xong 2026-07-30** (`STRATA-S7-CI-REPORT.md`): ✅ pin `rust-toolchain.toml` 1.96.0 + rustfmt/clippy (PR #29) · ✅ `cargo fmt --all` một lượt, `fmt --check` nay **sạch 0 hunk** trên main (PR #30, `e80806b`) · ⏳ workflow `ci.yml` (rust fmt/clippy/test + submitter tsc + **`npm run test:fixture`** đối chiếu Rust↔TS) đứng ở **PR #31 chưa merge**. **Chặn ở tầng QUYỀN, không phải kỹ thuật:** dep `lampnet-merkle-anchor` kéo từ repo **private** `LampNetCloud/Anchor`, `GITHUB_TOKEN` mặc định không đọc được repo khác ⇒ cần secret `ANCHOR_READ_TOKEN`; nhưng (1) fine-grained PAT chỉ tạo được trên web UI (không có API mint token) và (2) đặt secret **đòi quyền admin repo**, trong khi `lrybi` là `{admin: false, push: true}` trên **cả** `Strata` lẫn `Anchor`. ⇒ phải anh Đức: thêm khoá đọc, hoặc cấp admin. Đã hỏi ở comment PR #31. **Hướng "cho `Anchor` public" đã RÚT LẠI** — không đảo ngược được (bản đã clone/fork/index thì set private lại cũng không thu hồi), và đổi một quyết định cấp công ty lấy tiện lợi CI; đề xuất **deploy key** (không hết hạn, gắn repo `Anchor`) thay cho fine-grained PAT (tối đa 1 năm, gắn tài khoản cá nhân). Lặp lại đúng rào ở #2/#4 hồi 07-03, chỉ khác: lần đó **máy dev** thiếu quyền, lần này **runner CI** thiếu quyền — mà runner thì không add-collaborator được. Kèm đợt này: **test-vector chung Rust↔TS cho codec label 1234** (PR #33, `0fefc0b`) — xem report. **Property test P1–P7 (Tech §9.3) vẫn CHƯA có** — phần còn lại lớn nhất của S7. Trạng thái cũ: — là **cơ chế ép** cho mọi milestone sau, nên đứng trước S8+. Repo **chưa có workflow nào** (`gh pr checks 19` → "no checks reported"); `cargo fmt --all --check` **đỏ 8 hunk ngay trên main** (rustfmt 1.9.0 drift, không pin toolchain). Property test Tech §9.3 **P1–P7 hiện 0** (không có `proptest`/`quickcheck`) — trong khi test theo invariant §9.1 thì đủ INV-E1..E9. *Thứ tự có ràng buộc (issue #24):* pin toolchain → workflow (`test` + `clippy -D warnings` + `fmt --check` + `tsc`) → **PR fmt-sweep riêng** → mới bật `fmt --check` thành gate. *Xong khi:* CI xanh trên PR + 7 property P1–P7 chạy trong CI. |
| **S8** | Ghép đường đọc — DerivedIndex + checkpoint S3 + composite S4 vào daemon | Rust | P1 | ⬜ **CHƯA BẮT ĐẦU.** S2/S3/S4 đã CODE-COMPLETE nhưng **chưa nối vào daemon**: route đọc của S5 không dùng `ColumnarIndex`, `verify_two_tier` (S3) và `link_two_tier`/`ColumnSumTree` (S4) chưa lộ qua HTTP §3. Hiện chỉ là ghi chú trong ô S5, không phải milestone. *Xong khi:* `proof/version` trả **two-tier** khi seq nằm trong checkpoint đã đóng; query `FieldEquals`/`FieldLatest` chạy qua index (không brute-force); composite cha–con resolve được qua API; row-sum proof có route. |
| **S9** | ZK-lite hardening `state_root` — padding lá + blinding (Math §6.3) | Rust | P1 | ⬜ **CHƯA BẮT ĐẦU — có lỗ hở an ninh đang mở.** `src/state.rs:29 fval_hash(field_value_bytes)` **KHÔNG có salt** ⇒ hash anh em cố định giữa các version: (a) so khớp liên-proof biết trường nào đổi/không đổi, (b) **tấn công từ điển trên trường ít entropy** (boolean/enum) — đoán giá trị từ hash. Số lá cũng không đệm ⇒ lộ *khoảng* số trường. Math §6.3 đã ghi cách vá: đệm lá lên lũy thừa 2 bằng `H_dom("LN/STRATA/state/pad/v1", nonce)` + blinding `fvh_i = H_dom("LN/STRATA/state/fval/v1", salt_i ‖ value)` với `salt_i` mới mỗi version. **Lưu ý:** đổi công thức `fvh` là **đổi byte-layout** ⇒ phải qua `_CONTRACT.md` (CHỐT-4/tag) + version hoá, không sửa lặng. `src/privacy.rs` hiện chỉ phủ padding/decoy **kích thước blob** (INV-E9), KHÔNG phủ mục này. |
| **S10** | Migration dữ liệu tĩnh cũ → Strata 1-version (Tech §8) | Rust | P2 | ⬜ **CHƯA BẮT ĐẦU** (chỉ có dấu vết ở `src/refid.rs`). Cần `migrate_static(old_cid, owner_did)`: `content_cid = strip_class(old_cid)` (bỏ class byte = vá leak INV-E5 ngay lúc migrate), `data_class` cũ (Vault/Bulk) **chuyển vào state** chứ không vào định danh, bảng map `old_cid → ref_id` để link cũ vẫn resolve. §8.2: `parse_root_hash` giữ cả 3 nhánh (legacy `ln1q_` 8B · `lamp://ln1` 32B · `lnref1` 32B no-class), endpoint cũ chạy nguyên — **opt-in, không phá**. |

**Thứ tự phụ thuộc:** S1 (cố định byte-layout) → S3 (phụ thuộc S1 sink + tag) · S2 độc lập (build đã thông) · S4 độc lập, dựng trên S-state + `sumtree` Anchor (không phụ thuộc S1/S3).

**Thứ tự phụ thuộc (phần mới):** **S7** đứng trước mọi milestone sau (không có CI thì test của S6/S8/S9 chỉ đúng ở máy dev) · **S6**(a) độc lập, **S6**(b) chặn ngoài repo · **S8** cần S6(a) để proof lịch sử sống qua restart · **S9** độc lập nhưng đổi byte-layout ⇒ phải qua `_CONTRACT.md` trước · **S10** sau cùng, không chặn ai.

**Treo (chưa đặt milestone, có lý do):**
- **ZK đầy đủ cho field-proof** (Math §6.3 đoạn cuối: nâng lên cam kết đại số + bằng chứng ZK). Trước đây không có nhà; từ 2026-07-30 **giao với Glint** — `VeDataIO/Specs#22` định vị lại Glint = thư viện primitive ZK-proof (P3 membership/field-proof). Chờ Glint chốt catalog rồi mới quyết Strata gọi primitive hay tự dựng. Không tự làm trước.
- **Mức phân tán tầng (c) "phân tán chọn lọc"** (Feat §7): phụ thuộc phần cross-repo của **S6**(b) — không tách milestone riêng khi Mirage chưa có `EncryptedDistributed`.

**Lưu vết phạm vi ngoài bảng S1–S5:** bốn module đã build nhưng không nằm trong milestone nào — `src/privacy.rs` (decoy/padding kích thước, INV-E9 · Feat §9/Tech §7.6), `src/audit.rs` (`AuditEntry`, audit-log bất biến · Feat §10/Tech §1.6b/API §2.6), `src/refid.rs` (`ref_id` không lộ loại, INV-E5 · Tech §2), `src/field_policy.rs`. Ghi vào đây để bảng milestone không còn hẹp hơn phạm vi thật.


---

## 3. Điểm cần đối chiếu trước khi cố định byte-layout S1 — ✅ ĐÃ CHỐT (lưu vết)

> **Cả hai điểm dưới đây anh Đức đã chốt ở comment issue #1 (2026-07-04) và S1/S3 đã code+merge theo đó** — giữ lại làm lưu vết, không còn là việc treo:
> 1. **Byte-layout anchor → phương án A** (giữ `_CONTRACT.md`): `anchor = (ref_id, head_version_hash, mmr_root, seq)` = 104 byte. `state_root` đã cam-kết-bắc-cầu qua `head_version_hash`, không thêm 32 byte on-chain.
> 2. **Tag checkpoint:** trong cài đặt thật, "checkpoint sub-MMR entry" và "batch entry gộp lô" là **một** khái niệm (`batch.rs`), nên chỉ dùng **một** tag `LN/STRATA/entry/v1` — bảng CHỐT-2 giữ nguyên, không cần thêm tag thứ hai.

Mô tả trong comment issue lệch với `spec/_CONTRACT.md` hiện tại — cần thống nhất bản chuẩn trước khi cố định datum neo.

1. **Byte-layout anchor.**
   - Comment issue #1: `{version_hash, mmr_root, state_root, seq}`.
   - `_CONTRACT.md`: `anchor = (ref_id, head_version_hash, mmr_root, seq)` = 32+32+32+8 = **104 byte**.
   - Khác biệt: `state_root` (comment) so với `ref_id` (`_CONTRACT.md`).
2. **Chuỗi domain-tag checkpoint sub-MMR.**
   - Comment issue #3: `LN/STRATA/checkpoint/entry/v1`.
   - `_CONTRACT.md` bảng CHỐT-2 hiện có `LN/STRATA/entry/v1` (Batch entry sub-MMR gộp lô).
   - Chuỗi cuối cần đóng vào `_CONTRACT.md` (CHỐT-2 là bảng domain-tag DUY NHẤT).

---

## 4. Ràng buộc kỹ thuật cố định (`_CONTRACT.md`)

- **Hash nền:** BLAKE3 32 byte. Domain-separated `H_dom(tag, x) = BLAKE3(tag ‖ 0x00 ‖ x)`. RFC 6962 prefix (leaf `0x00`, internal `0x01`). Dup-leaf guard (CVE-2012-2459): số leaf lẻ xử lý theo MMR carry, không copy leaf cuối.
- **CHỐT-1:** `version_hash = H_dom("LN/STRATA/ver/v1", canonical(core))` KHÔNG gồm `sig`. Tamper-evidence từ chữ ký canonical Ed25519 low-S trên `version_hash`.
- **CHỐT-3:** `mmr_root = H_dom("LN/STRATA/mmr/root/v1", u64_be(n) ‖ bag_of_peaks)` — commit số lá `n` trước khi bag peaks.
- **CHỐT-4:** `value_cid` của một trường state là `content_cid` thuần (không class byte / doc_type) — nếu không sẽ leak loại qua field-proof (INV-E5/E6).
- **CHỐT-5:** `Did` lưu `[u8;32]` (băm DID PhoenixKey); verify chữ ký cần ánh xạ `Did → pubkey` qua key-registry lampnet-join/PhoenixKey, không giả định `Did == pubkey`.
